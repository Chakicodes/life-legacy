{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Typecheck and Lint",
      "type": "shell",
      "command": "npm run -s lint && npm run -s typecheck",
      "group": "build",
      "problemMatcher": ["$eslint-stylish", "$tsc"]
    }
  ]
}
