{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Typecheck and Lint",
			"type": "shell",
			"command": "npm run -s lint && tsc -p tsconfig.json",
			"problemMatcher": [
				"$eslint-stylish"
			],
			"group": "build"
		}
	]
}